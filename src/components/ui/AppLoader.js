import React from 'react'
import {StyleSheet,View,ActivityIndicator, TouchableHighlightComponent} from 'react-native'
export const=()=>{
    <View style={styles.center}>
        <ActivityIndicator color={TouchableHighlightComponent.MAIN_COLOR} size="large"></ActivityIndicator>
    </View>
}
const styles=StyleSheet.create({
    center:{
        flex:1,
        justifyContent:'center',
        alignItems:'center'
    }
})